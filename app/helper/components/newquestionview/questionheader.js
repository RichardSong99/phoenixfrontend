import { QuestionContext } from '../../context/questioncontext';

export default function QuestionHeader({ mode, review }) {
    return (
        <div className=''>
            
        </div>
    )
}