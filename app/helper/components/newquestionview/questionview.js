import { QuestionContext } from '../../context/questioncontext';

export default function QuestionView({ review }) {
    return (
        <div className=''>
            
        </div>
    )
}